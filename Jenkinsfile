pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                echo "Welcome to Jenkins Enviroment"
                sh 'echo using shell within Jenkinsfile'
                sh 'new update2'
            }
        }
    }
}